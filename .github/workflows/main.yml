name: CSV to JSON Automation

on:
  push:
    paths:
      - "call-center-dashboard/dist/data/*.csv"
      - "call-center-dashboard/scripts/csv_to_json.py"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pandas

     - name: Convert CSV to JSON
  working-directory: scripts
  run: python csv_to_json.py

      - name: Commit generated JSON
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add call-center-dashboard/dist/data/*.json
          git commit -m "Auto update dashboard JSON" || echo "No changes"
          git push
